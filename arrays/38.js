//Check if array contain specific value
const array = [5,15,25,35];
const hasValue = array.includes(25);
// check the condition
if(hasValue) {
    console.log('Array contains a value.');
} else {
    console.log('Array does not contain a value.');
}
